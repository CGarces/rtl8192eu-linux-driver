language: c
compiler: gcc
dist: bionic
os: linux
arch: arm64

script: make CC=$CC CONFIG_PLATFORM_ARM_AARCH64=y CONFIG_PLATFORM_I386_PC=n CONFIG_MP_VHT_HW_TX_MODE=n

addons:
  apt:
    packages:
      #Force update to GCC-7.5 in order to compile Kernels >= 5.4.
      - gcc-7
      - bc
      - linux-headers-$(uname -r)

